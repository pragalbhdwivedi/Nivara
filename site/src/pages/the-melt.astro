---
import CinematicDocLayout from "../layouts/CinematicDocLayout.astro";
import { getCanonRoute } from "../utils/canon.js";

const route = "/the-melt";
const { title, sections, toc, meta } = await getCanonRoute(route);
---

<CinematicDocLayout title={`Nivara | ${title}`} heading={title} sections={sections} toc={toc} meta={meta} />
